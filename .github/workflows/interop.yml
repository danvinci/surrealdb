name: Interop
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  python-julia:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: julia-actions/setup-julia@v2
        with:
          version: '1'
      - uses: julia-actions/cache@v2
      - uses: julia-actions/julia-buildpkg@v1
      - name: Instantiate test env
        run: julia --project=test -e 'using Pkg; Pkg.develop(PackageSpec(path=pwd())); Pkg.instantiate()'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install surrealdb-py
        run: pip install --upgrade pip && pip install surrealdb
      - name: Start SurrealDB
        run: |
          docker run -d -p 8000:8000 \
            --name surrealdb \
            surrealdb/surrealdb:latest \
            start --user root --pass root memory
      - name: Wait for SurrealDB
        run: |
          for i in $(seq 1 30); do
            curl -s -X POST http://localhost:8000/rpc \
              -H "Content-Type: application/json" \
              -d '{"id":1,"method":"version","params":[]}' && break
            sleep 1
          done
      # Forward: Python writes, Julia reads.
      - name: Forward — Python writes fixtures
        run: python test/interop/write_test_data.py
        env:
          SURREALDB_URL: ws://localhost:8000/rpc
      - name: Forward — Julia reads, asserts equal
        run: julia --project=test -e 'include("test/interop/test_interop.jl")'
        env:
          SURREALDB_URL: ws://localhost:8000

      # Reverse: Julia writes, others read. Same fixtures, different
      # direction — catches serialization drift in our writer that the
      # forward direction can't see.
      - name: Reverse — Julia writes fixtures
        run: julia --project=test test/interop/write_test_data_julia.jl
        env:
          SURREALDB_URL: ws://localhost:8000
      - name: Reverse — Python reads, asserts equal
        run: python test/interop/read_test_data.py
        env:
          SURREALDB_URL: ws://localhost:8000/rpc

      # Cross-check via a second SDK. Same fixtures, Go decoder this time.
      # If only one of Python or Go disagrees, the bug is in that SDK's
      # decoder; if both disagree, the bug is in our encoder.
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Reverse — Go reads, asserts equal
        run: |
          mkdir -p /tmp/go-interop && cd /tmp/go-interop
          go mod init interop 2>/dev/null || true
          go get github.com/surrealdb/surrealdb.go@latest
          cp ${{ github.workspace }}/test/interop/read_test_data.go main.go
          go run main.go
        env:
          SURREALDB_URL: ws://localhost:8000
