# Built libsurreal binary; not part of source distribution
libsurrealdb_c.dylib
libsurrealdb_c.so
libsurrealdb_c.dll
*.so
*.dylib
*.dll

# Library packages don't commit Manifest — would lock users to our
# transitive deps. Project.toml is the public contract.
Manifest.toml

# Local editor / agent state
.claude/

# Julia precompile output
*.cov
*.jl.cov
*.jl.mem
deps/build.log

# OS
.DS_Store

# Documenter.jl build output
docs/build/
docs/Manifest.toml
